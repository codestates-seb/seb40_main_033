import styled from 'styled-components';

function Footer() {
	return <FooterContainer>푸터욤</FooterContainer>;
}

const FooterContainer = styled.footer`
	background-color: var(--gray-500);
	width: 100%;
	height: 365px;
`;

export default Footer;
